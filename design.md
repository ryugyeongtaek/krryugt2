# SCM 시스템 디자인 가이드

## 1. 디자인 방향

이 시스템은 한국후지필름BI의 월간 발주계획과 공급망 분석을 위한 업무용 화면이다. 사용자가 많은 데이터를 빠르게 비교하고, 위험 상태와 다음 행동을 즉시 이해할 수 있도록 **밝고 정돈된 화면**을 기본으로 한다.

제공된 HTML의 고밀도 분석 대시보드 패턴은 다음 영역에 활용한다.

- 좌측 사이드바와 상단 바를 통한 명확한 업무 영역 구분
- KPI 카드와 분석 테이블을 한 화면에서 비교하는 정보 구조
- 차트 헤더의 기간 선택, 필터, 시나리오 조정 패널
- 선택 상태와 중요 지표를 짙은 색 표면으로 강조하는 방식

다만 전체 화면을 어두운 테마로 만들지 않는다. 기본 표면은 화이트와 연한 그린으로 구성하고, 짙은 그린은 활성 메뉴·핵심 지표·주요 액션처럼 사용자의 시선을 모아야 하는 곳에만 사용한다.

핵심 인상은 다음 세 단어로 정의한다.

> Clean · Calm · Decisive

## 2. 색상 시스템

### 2.1 기본 토큰

모든 색상은 `app/globals.css`의 CSS 변수로 관리한다. 화면 컴포넌트에서 임의의 색상값을 직접 사용하지 않는다.

| 토큰 | 권장값 | 용도 |
|---|---|---|
| `--canvas` | `#f4faf7` | 앱 전체 배경, 페이지 캔버스 |
| `--surface` | `#ffffff` | 카드, 표, 입력 영역 |
| `--surface-subtle` | `#edf7f3` | 표 헤더, 보조 패널, 선택된 낮은 강조 |
| `--surface-emphasis` | `#e3f2ec` | 활성 행, 필터 선택 상태, 안내 영역 |
| `--primary` | `#34a78a` | 기본 브랜드색, 링크, 주요 버튼 |
| `--primary-hover` | `#278c73` | hover, focus, 눌림 상태 |
| `--primary-strong` | `#176653` | 짙은 강조 표면, 활성 메뉴, 중요한 수치 |
| `--primary-dark` | `#104b3d` | 사이드바 또는 강조 패널의 가장 짙은 배경 |
| `--primary-soft` | `#d8eee6` | primary의 연한 배경, 배지 |
| `--ink` | `#17352f` | 기본 본문과 제목 |
| `--muted` | `#6d827b` | 설명, 보조 문구, 비활성 텍스트 |
| `--line` | `#dbeae4` | 카드와 표의 경계선 |
| `--line-strong` | `#bdd8ce` | 입력 포커스, 강조 경계선 |
| `--white` | `#ffffff` | 짙은 표면 위 텍스트 |

### 2.2 상태 색상

상태 색상은 primary 계열과 시각적으로 구분되어야 한다. 상태를 색상만으로 전달하지 말고 텍스트나 아이콘을 함께 표시한다.

| 상태 | 기본색 | 연한 배경 | 사용 예 |
|---|---|---|---|
| 정상 | `#23815f` | `#e7f6ee` | SAFE, 정상, 계획 범위 |
| 주의 | `#ad711c` | `#fff5df` | MEDIUM, 표본 부족, 확인 필요 |
| 위험 | `#c34b4b` | `#fff0ef` | CRITICAL, 지연, 조회 실패 |
| 정보 | `#3678a8` | `#edf5fb` | 안내, 분석 기준, 참고 |
| 계산 불가 | `#72817c` | `#f0f3f2` | UNKNOWN, 데이터 부족 |

상태색 규칙은 데이터 규칙과 연결한다. `null`, `NO_USAGE`, `NO_LEADTIME`을 임의의 숫자나 정상 상태로 표시하지 않고, `계산 불가` 또는 사유 코드로 명확하게 표현한다.

## 3. 레이아웃

### 3.1 앱 구조

```text
app-shell
├── sidebar       232~250px, 업무 메뉴와 현재 위치
└── main
    ├── topbar     68~76px, 화면 제목과 전역 동작
    └── content    최대 1440px, 분석 콘텐츠
```

- 사이드바는 데스크톱에서 고정 폭을 유지한다.
- 상단바는 화이트 표면과 얇은 하단 경계선을 사용한다.
- 콘텐츠 영역은 `--canvas` 배경 위에 `card`를 배치한다.
- 페이지 좌우 여백은 데스크톱 32~42px, 좁은 화면 20px을 기준으로 한다.
- 한 화면에서 가장 중요한 지표가 먼저 보이도록 제목 → KPI → 분석 표/차트 순서를 지킨다.

### 3.2 그리드

- 기본 간격은 16px이다.
- KPI는 `grid-3` 또는 `grid-4`를 사용하되, 카드 수가 적으면 넓게 배치한다.
- 상세 분석은 메인 콘텐츠와 보조 패널을 `2:1` 또는 `3:1` 비율로 나눈다.
- 모바일에서는 모든 그리드를 한 열로 접고, 보조 패널을 주요 콘텐츠 아래에 배치한다.
- 표는 화면을 억지로 축소하지 않고 가로 스크롤을 허용한다.

## 4. 간격과 형태

### 4.1 간격 스케일

| 이름 | 값 | 용도 |
|---|---:|---|
| `xs` | 4px | 아이콘과 텍스트 사이 |
| `sm` | 8px | 배지 내부, 작은 그룹 |
| `md` | 16px | 카드 내부, 컴포넌트 간격 |
| `lg` | 24px | 섹션 간격 |
| `xl` | 32px | 페이지 주요 영역 |
| `2xl` | 40px | 큰 페이지 여백 |

### 4.2 모서리와 그림자

- 기본 카드와 입력: `10~14px` 둥근 모서리
- 배지와 상태 표시: 완전한 pill 형태
- 버튼: `8~10px`
- 기본 상태에서는 그림자를 최소화하고 `--line` 경계선으로 영역을 구분한다.
- 떠 있는 메뉴, 팝오버, 강조 패널에만 매우 약한 그림자를 사용한다.

## 5. 타이포그래피

- 기본 글꼴은 현재 프로젝트의 `Noto Sans KR`을 유지한다.
- 페이지 제목: 24~28px, 700~800 weight
- 섹션 제목: 17~20px, 700 weight
- 본문: 14px, 400~500 weight
- 보조 설명: 12~13px, `--muted`
- KPI 숫자: 28~36px, 700~800 weight
- 표 숫자와 코드: tabular 숫자와 고정폭 숫자 스타일을 우선한다.
- 영문 라벨은 짧게 사용하되, 사용자에게 보이는 화면 문구는 한국어를 우선한다.

제목은 한 화면에 하나의 주 제목만 두고, 섹션 제목은 내용의 목적이 드러나게 작성한다. 예: `공급처별 리드타임`, `소진 위험 품목`, `입고 예정 현황`.

## 6. 컴포넌트 규칙

### 6.1 사이드바

- 기본 배경은 `--primary-dark` 또는 화이트 중 한 가지를 앱 전체에서 일관되게 선택한다.
- 이번 시스템에서는 HTML의 강조감을 살리기 위해 짙은 그린 사이드바를 권장한다.
- 브랜드 마크는 `--primary` 또는 `--white`를 사용한다.
- 기본 메뉴는 반투명한 화이트 텍스트, 활성 메뉴는 `--primary` 배경과 화이트 텍스트를 사용한다.
- 메뉴에는 아이콘과 한국어 라벨을 함께 표시한다.
- 활성 상태는 배경, 텍스트, 좌측 표시선 중 최소 두 가지로 전달한다.

### 6.2 상단바

- 현재 화면의 제목과 짧은 설명을 왼쪽에 둔다.
- 검색, 알림, 도움말, 사용자 정보 등 전역 동작은 오른쪽에 둔다.
- 주요 버튼은 한 화면에서 하나만 primary로 둔다.
- 상단바의 배경은 화이트로 유지해 콘텐츠와 시각적으로 분리한다.

### 6.3 KPI 카드

```text
card
├── metric-label  지표 이름
├── metric-value  핵심 숫자 또는 상태
└── metric-foot   기준, 변화량, 보조 설명
```

- 카드 안에는 한 가지 지표만 담는다.
- 핵심 숫자는 크게, 단위는 숫자와 시각적으로 분리한다.
- 긍정/부정 변화는 색상과 화살표, 짧은 문구를 함께 사용한다.
- 중요도가 높은 KPI만 좌측 accent 선 또는 연한 그린 표면으로 강조한다.

### 6.4 카드와 섹션

- `card`는 콘텐츠의 독립적인 목적이 있을 때 사용한다.
- 카드 제목 영역은 제목, 기준 설명, 우측 동작을 한 줄에 배치한다.
- 카드마다 테두리와 배경을 모두 강하게 사용하지 않는다. 기본은 화이트 + 얇은 선이다.
- 긴 설명은 제목 아래에 한 문장으로 제한한다.

### 6.5 테이블

- 표 헤더는 `--surface-subtle`, 본문은 `--surface`를 사용한다.
- 숫자는 오른쪽 정렬, 상태·이름은 왼쪽 정렬한다.
- 행 hover는 `--surface-emphasis`를 약하게 사용한다.
- 핵심 상태는 `tag`, `text-good`, `text-danger`, `muted` 같은 기존 클래스로 표현한다.
- 빈 결과와 조회 오류는 구분한다. 오류는 위험색과 해결에 필요한 안내를 함께 표시한다.
- 열이 많아도 행 높이를 과도하게 줄이지 말고, 기본 44~52px을 유지한다.

### 6.6 버튼과 입력

- primary 버튼: `--primary` 배경 + 화이트 텍스트
- secondary 버튼: 화이트 배경 + `--line-strong` 경계선
- quiet 버튼: 투명 배경 + `--muted` 텍스트
- hover는 배경색 변화, focus는 `--primary` 외곽선으로 표시한다.
- 파괴적 동작은 위험색을 사용하고, 저장·확정 동작과 시각적으로 구분한다.
- 입력 높이는 38~42px, 라벨은 입력 위에 둔다.

### 6.7 배지와 상태

- 배지는 짧은 상태명만 표시한다.
- `SAFE`, `CRITICAL` 같은 내부 코드는 필요하면 한국어 설명과 함께 표시한다.
- 데이터 없음과 계산 불가는 회색 계열로 표현하며 정상으로 오해할 수 있는 초록색을 사용하지 않는다.

## 7. 제공 HTML의 강조 패턴 적용 기준

제공 HTML의 다음 요소를 선택적으로 재사용한다.

| HTML 패턴 | 적용 방식 |
|---|---|
| 짙은 다크 사이드바 | 짙은 그린 사이드바로 변환하여 앱 내비게이션에 사용 |
| 활성 메뉴의 강한 배경 | `--primary` 배경과 화이트 텍스트로 사용 |
| KPI 4열 행 | 분석 화면의 핵심 요약 영역에 사용 |
| 차트 기간 선택 | `1M`, `3M`, `1Y` 형태의 segmented control로 사용 |
| 시나리오 패널 | 상세 분석 화면의 우측 보조 패널에 사용 |
| 다크 강조 패널 | 저장·확정·시뮬레이션처럼 집중이 필요한 영역에만 사용 |
| 고밀도 표 | 열 정렬과 행 hover만 유지하고, 배경은 밝게 조정 |

다크 표면을 페이지 전체에 반복해서 사용하지 않는다. 한 화면에서 짙은 강조 표면은 사이드바 외에 최대 1~2개 영역으로 제한한다.

## 8. 인터랙션과 접근성

- 모든 버튼과 입력은 키보드로 접근할 수 있어야 한다.
- `:focus-visible` 상태를 제거하지 않는다. primary 색상의 2px outline을 사용한다.
- 텍스트와 배경의 대비를 충분히 확보한다. 연한 그린은 큰 면적의 배경에만 사용하고 본문 텍스트에는 사용하지 않는다.
- 상태는 색상 외에도 텍스트, 아이콘, 모양 중 하나를 추가한다.
- hover에만 의존하지 말고 active, selected, disabled 상태를 별도로 정의한다.
- 로딩 중에는 레이아웃이 흔들리지 않도록 고정 높이 또는 skeleton을 사용한다.
- 모바일에서 사이드바는 접을 수 있어야 하며, 핵심 정보와 주요 동작이 먼저 보여야 한다.

## 9. 적용 순서

1. `app/globals.css`의 기존 색상 변수를 위 토큰 체계로 정리한다.
2. `app/layout.tsx`와 앱 셸에서 사이드바·상단바의 표면과 활성 상태를 통일한다.
3. `card`, `metric`, `section`, `analysis-table`, `tag` 등 공통 클래스부터 적용한다.
4. `components/analysis/*`의 공통 컴포넌트에 테이블·상태·오류 표현 규칙을 반영한다.
5. `app/analysis/*` 화면은 화면별 계산 로직을 변경하지 않고 스타일 클래스만 일관되게 적용한다.
6. 새 화면은 이 문서의 토큰과 컴포넌트 규칙을 먼저 따르고, 새로운 색상·간격·컴포넌트가 필요할 때만 문서를 함께 갱신한다.

## 10. 완료 기준

디자인 적용 후 다음을 확인한다.

- 모든 분석 화면에서 배경, 카드, 제목, 표, 버튼의 색상 체계가 동일하다.
- `#34a78a` 계열이 브랜드 포인트로 인식되지만 화면이 과도하게 채도가 높지 않다.
- 짙은 그린 강조 영역이 핵심 상태와 주요 행동을 명확히 안내한다.
- 조회 오류, 빈 결과, 계산 불가가 서로 다르게 보인다.
- 데스크톱과 좁은 화면 모두에서 콘텐츠가 잘리지 않는다.
- 주요 상태와 버튼을 색상만으로 구분하지 않아도 이해할 수 있다.
